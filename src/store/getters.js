export default{
    UI_open(state) {
        return state.UI.open;
    },
    APP_tags(state) {
        return state.APP.tags;
    },
    APP_scenes(state) {
        return state.APP.scenes;
    },
    APP_movies(state) {
        return state.APP.movies;
    }
}